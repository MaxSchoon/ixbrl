# Local agent configuration (machine-specific) — never publish
.agents/
.claude

# Python virtual environments
.venv/

# Python bytecode artifacts
__pycache__/
*.py[cod]

# Session ledgers and other agent-process records are private to the
# operator and are never published with the skill.
**/session-ledger/
docs/session-ledger/
session-ledger/

# Local-only working files; never published
paths/
