# Dev/CI toolchain, pinned so the standardization gate is reproducible — the
# same reason the GitHub Actions in .github/workflows/ci.yml are SHA-pinned.
# Dependabot bumps these (see .github/dependabot.yml, `pip` ecosystem).
#
# Nothing here is needed to *use* the skill; the skill itself is prose and
# static assets. These are only for running the checks locally and in CI.

# Standardization gate: linter + formatter for scripts/ and tests/ (ruff.toml).
ruff==0.16.3

# Runtime dependency of scripts/check_facts.py only.
lxml==6.1.1

# Type gate. pyrightconfig.json declared a type standard from the start but
# nothing ran it; CI now does. lxml ships no inline types, so the stubs are
# what make the declared standard achievable rather than aspirational.
pyright==1.1.411
lxml-stubs==0.5.1

# CommonMark parser for the reference-structure and move-integrity gates.
# These must not use line-oriented regex: the reference files contain `#` shell
# comments inside bash fences, which a regex heading matcher miscounts.
markdown-it-py==4.2.0

# CA bundle for scripts/dts_profile.py's online walks. python.org macOS builds
# ship no system CA bundle, and the script uses certifi's when it is importable.
certifi==2026.6.17
